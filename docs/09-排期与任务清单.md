# 09 · 排期与任务清单

**起始：2026-08-13（周四）｜结束：2026-09-23**

> **范围以 [11 MVP 边界](11-MVP边界.md) 为准。** 本文档是它的逐周展开。
> 蓝图里但不在本排期的功能，在 [11 §5 Backlog](11-MVP边界.md) 排队，不是被删除。

秋招提前批已在进行，正式批 8–9 月。**M0 上线（8/26）是硬门槛**——
第一版简历必须能写上可访问的 demo 链接。

| 里程碑 | 区间 | 交付 | 硬门槛 |
|---|---|---|---|
| M0 | 08-13 ～ 08-26 | 入库 + dense 检索 + 溯源 + 拒答 + 安全 + 上线 + 基线 | 公网 demo + 基线数字 |
| M1 | 08-27 ～ 09-09 | 混合检索 + rerank + 固定综述工作流 + Judge 校准 | 第一批真实增量数字 |
| M2 | 09-10 ～ 09-23 | 三档路由（或记忆）+ CI 门禁 + 输出 | 台账有厚度，含负优化 |

**贯穿全程**：从 W1 起自己每天用。dogfooding 天数是成功标准之一，也是 badcase 唯一来源。

---

## W1｜08-13 ～ 08-19：入库链路打通

> 本周纪律：**评测标注同步启动，不能等系统做完再标**（R3）。

### 环境（第 1 天做完）
- [x] 安装 OrbStack（Docker 29.4.0 已验证）
- [x] `uv python pin 3.12`（当前锁定 3.12.13）
- [x] `deploy/docker-compose.yml`：postgres+pgvector、redis（**minio/langfuse 推到 M1**）
- [x] 初始化 SQL：[03](03-数据模型.md) 的知识层 + 评测层 + `llm_calls` +
      `agent_runs` / `run_events`（SSE 基础）；**其余 Agent 四表推到 M1**；启用 `vector`
      （`zhparser` 随 M1 词法检索引入）
- [x] 后端骨架：FastAPI + Pydantic Settings + structlog(trace_id) + 健康检查

### 模型网关（先于业务代码，否则后期改不动）
- [x] `llm/gateway.py`：`complete()` / `stream()` / `embed()` 统一入口
- [ ] `llm/router.py` + `config/routing.yaml`：**M0 只配 `main` + `external` 两档**
- [ ] `llm/budget.py`：**调用前**检查每日成本上限（[12 §2.2](12-安全与部署.md)）
- [x] 每次调用写 `llm_calls`

### 入库链路 ★ 本周主线
- [x] `sources` + `local_dir` connector（含 realpath 校验、持久化增量游标、文件级失败隔离）
- [x] **PDF 解析**（MinerU）：双栏重排序、表格转 Markdown、公式转 LaTeX、页眉页脚剔除
  - [x] PyMuPDF 文本层基线：基础双栏排序、重复页眉/页脚过滤、页码与 bbox 定位
  - [x] auto 路由、结构/文本质量门控、超时清理、显式回退与解析元数据
  - [x] 真实 PDF 跑批 + bbox 抽样叠图；跨页表格/扫描件专项样本继续补充
- [x] **Markdown 解析**
- [x] 输出 `document_versions.full_text` + `parsed_blocks`
      （NFC Unicode code-point `char_start/end`）+ `parsed_block_locations`
      （`bbox_norm`、`page_width/height`、`rotation`、`coord_origin`）
- [x] 候选版本完整成功后再事务激活；验证新版解析失败时旧版仍可检索，
      且慢到的旧候选不能覆盖已激活的新候选
- [x] **PDF 解析跑在独立子进程**，带文件大小、页数、内存/CPU/超时上限
- [x] `heading` 分块（**只做这一种**）→ `chunks`，记录 block 区间与字符区间
- [ ] bge-m3 embedding（dense + sparse 都存，**sparse 不建检索路径**）
- [x] dense 检索 + 生成 + 引用标记解析（引用锚 `block_id`）
- [ ] **先灌数据，后建 HNSW 部分索引**

### 语料与标注
- [ ] 导入 ≥ 200 篇真实资料，抽样 20 条人工核验解析质量
- [x] 定 `gold_spans` 标注规范 + 本地标注工具（UTF-16 选区由后端转 code-point，入库 quote 校验；PDF bbox 预览）
- [ ] **标注 20 条**（覆盖 single_hop / multi_hop / table / unanswerable）

**W1 交付**：命令行/API 能问能答带引用 ｜ 20 条标注

---

## W2｜08-20 ～ 08-26：上线 ★ 硬门槛

### 流式与恢复（[ADR-0007](adr/0007-agent幂等与事件溯源.md)）
- [ ] `agent_runs` + `run_events` 两表（普通对话也走 run）
- [ ] Arq worker：任务**不依附 HTTP 连接**
- [ ] `POST /runs` → `GET /runs/{id}/events?after_seq=N`（先补历史再续流）
- [ ] run worker 租约 + heartbeat + watchdog；普通生成崩溃显式失败，不静默重复计费
- [ ] SSE 信封 `{id: "run:seq", run_id, seq, type, data}`，`id` 用作 `Last-Event-ID`
- [ ] `messages.status` = streaming/completed/failed/cancelled

### 前端（阶段 1–2）
- [ ] Next.js + shadcn + Vercel AI SDK
- [ ] 对话流：流式消息 + 引用卡片
- [ ] 右栏原文预览：按 `bbox_norm` × 实际渲染尺寸定位高亮
- [ ] 资料库页：列表 + 导入/解析状态
- [ ] **边界情况 B1 / B2 / B3 自测通过**（刷新恢复、断线续传、关页面任务继续）

### 拒答
- [x] 检索分数阈值拒答（阈值先拍，M1 做 ROC）
- [ ] 前端体面拒答：给"基于通用知识回答"的一键切换

### 安全 ★ 上线前置（[12](12-安全与部署.md)）
- [ ] demo admin 登录 + demo 只读 session；对话/run/SSE 按 session 做对象级鉴权
      （私人语料只存在本地 owner 环境）
- [ ] per-IP 限流 + per-session 配额 + 每日费用熔断
- [ ] 上传：MIME 嗅探白名单、50MB、500 页、120s 超时
- [ ] 路径穿越防护；SSRF 防护（web_clip 若未做则禁用该 connector）
- [ ] **私人语料与演示语料物理隔离**（独立 PG 实例，无同步路径）
- [ ] 走完 [12 §5 上线检查清单](12-安全与部署.md)，逐条打勾

### 评测基线
- [ ] 标注补到 **40 条**
- [x] `gold_spans` → chunk 的区间映射（θ=0.5）+ quote/stale 校验
- [x] dense 规则轨指标：span-level Recall@k / budget Recall / nDCG@k / α-nDCG@k / MRR / context precision / 拒答检出
- [ ] 生成规则轨补充 `citation_validity` 与答案约束检查
- [ ] 人工检查 40 条引用是否真正支撑答案（M1 才用校准 Judge 自动化）
- [x] `eval/dense_baseline.py` 跑批骨架（DB 结果、模型身份/config hash、fallback=false）
- [ ] 40 条人工集跑批 → **正式基线数字入台账**

### 上线
- [ ] 云主机部署后端 + PG + Redis；前端 Vercel；走商用 API
- [ ] README：这是什么、怎么用、demo 链接

**W2 交付**：🔗 公网 demo ｜ 简历第一版可投
**可讲的数据**：dense-only 基线的 Recall@10 / nDCG@10 / 人工引用准确率 / 拒答率

---

## W3｜08-27 ～ 09-02：混合检索 + 四策略对照

### 检索升级
- [ ] 词法检索（PG `ts_rank_cd` + zhparser）——
      **注意命名：这不是 BM25**（[04 §4.1](04-RAG设计.md)）
- [ ] RRF 融合
- [ ] rerank（bge-reranker-v2-m3）
- [ ] 查询改写：指代消解 + 多查询扩展（引入 `light` 档）
- [ ] `hnsw.iterative_scan` 配置，验证过滤条件下的召回不塌

### 四策略对照 ★ 第一个正式实验
- [ ] 建 `fixed` / `recursive` / `semantic` 三套 chunk（`heading` 已有）
- [ ] **每策略一个部分 HNSW 索引**（[03 §4.1](03-数据模型.md)）
- [ ] E1：同一份 `gold_spans` 给四策略打分
- [ ] E1 主口径使用固定 token budget，同时报 context precision / 冗余率 / 端到端质量
- [ ] E2 混合检索增益 / E3 rerank 值不值 / E4 查询改写增益
- [ ] 每个实验一份台账，含假设、结果、归因

### 评测扩充
- [ ] 标注 → 120 条，dev/test 拆分（dev 100 / test 20）
- [ ] `eval/compare.py` 快照 diff
- [ ] paired bootstrap 置信区间（替代"三次取中位数"）

**W3 交付**：混合检索上线 ｜ 四策略结论
**可讲的数据**：分块策略对照表、混合检索增量、rerank 的质量-延迟权衡

---

## W4｜09-03 ～ 09-09：固定综述工作流 + Judge 校准

### Agent（**固定流程，不是通用 Agent**）
- [ ] 补齐 Agent 四表：`agent_plan_steps` / `agent_attempts` / `tool_invocations` / `agent_checkpoints`
- [ ] LangGraph 图：固定流程 筛选→抽卡→分组→对比→生成→写回
- [ ] `AgentState` 可序列化 + Postgres checkpointer
- [ ] 工具：`search_knowledge` / `list_documents` / `extract_card` / `compare_docs` / `write_note`
- [ ] **面向模型的错误信息**（约束 4）——最便宜、收益最大，优先做
- [ ] **副作用幂等协议**（[03 §6.3](03-数据模型.md)）：`write_note` 必须走 `tool_invocations`
- [ ] 幂等租约回收：测试过期 `in_flight` / `failed` 可 CAS 重试，原子 rename 后崩溃不重复写
- [ ] HITL：`write_note` 强制确认；**验证 interrupt 恢复后不重复写入**
- [ ] 预算熔断（token / 调用数 / 墙钟）
- [ ] 断点续跑：杀进程重启后从 checkpoint 继续（**录进 demo 视频**）
- [ ] 前端 Agent 时间线（消费 `run_events`）+ 中断恢复
- [ ] 边界情况 B4–B10 自测

### Judge 校准 ★ 评测可信度的地基
- [ ] 人工标注 80 条（覆盖全部 7 类）
- [ ] `heavy` 档当 Judge → 一致率 + **quadratic weighted Kappa** + 混淆矩阵
- [ ] 不达标则改 rubric 迭代（≤3 轮），记录分歧归因
- [ ] 评测跑批**默认禁用 fallback**，记录 `actual_models`

### Agent 实验
- [ ] A1 面向模型的错误信息：对 `tool_error_rate`、`recovery_rate` 的影响
- [ ] A2 工具描述含失败样例：对工具选择准确率的影响

**W4 交付**：综述工作流可用且可靠 ｜ Judge 可信
**可讲的数据**：Kappa 值、恢复率、幂等验证结果

---

## W5｜09-10 ～ 09-16：三档路由 + CI 门禁

> **M2 二选一**：默认走**选项 B（三档路由）**，理由见 [11 §4](11-MVP边界.md)。
> 若改选记忆系统，把本周前半替换为记忆四操作 + 时序冲突消解 + `/memory` 页。

### 三档模型路由
- [ ] 路由表补齐 `light` / `main` / `heavy` 三档 + fallback 链
- [ ] 置信度驱动的动态升档（结构化抽取）
- [ ] 精确缓存 + Prompt 缓存（**语义缓存进 Backlog**）
- [ ] **实验 C：帕累托前沿**（C1–C8 八配置 × 3 次）
- [ ] **实验 J：Judge 降档**（`main` 能否替代 `heavy`）
- [ ] 成本口径按 [07 §7](07-模型路由与成本.md)：
      **整批 GPU wall time / 任务数**摊销，同时报吞吐、并发、利用率
- [ ] 成本看板

### CI 门禁
- [ ] `eval/gate.py`
- [ ] **PR：20–30 条确定性 smoke + regression（纯规则轨，无 LLM Judge）**
      —— CI runner 连不到自建集群，这是硬约束不只是成本问题
- [ ] **夜间：完整 dev 集 + Judge**（在本地/集群跑，结果回传）
- [ ] 门禁阈值：**先测噪声幅度再定阈值**
- [ ] PR 自动贴 diff 报告
- [ ] badcase 回流流程跑通

### 可观测
- [ ] Langfuse 接入（本周才需要，M0 推迟是对的）

**W5 交付**：三档路由 + 成本看板 + CI 门禁
**可讲的数据**：成本下降 %、帕累托曲线、Judge 降档结论、门禁拦截案例

---

## W6｜09-17 ～ 09-23：收口与输出

### 功能收尾
- [ ] E7 拒答阈值 ROC 调优 / E5 HNSW 参数 / E6 top-k
- [ ] 前端整体打磨
- [ ] `core-test` 集**首次也是唯一一次**跑批 → 最终数字
- [ ] 若进度超前，按 [11 §5](11-MVP边界.md) 解锁 Backlog #1

### 对外输出（别拖到最后一天）
- [ ] README：架构图、指标表、一条命令启动、demo 链接
- [ ] 博客 1：《混合检索到底提升了多少：一份可复现的实验记录》
- [ ] 博客 2：《给 Agent 建一道 CI 门禁》
- [ ] Demo 视频 3 分钟，**必含两个镜头**：断点续跑、正确拒答
- [ ] 简历按 [10](10-简历与面试.md) 模板填真实数字
- [ ] 对照 [10 §3](10-简历与面试.md) 的必答题自查

**W6 交付**：完整开源项目 + 2 篇博客 + 1 个视频 + 简历定稿

---

## 每周日：范围复核（15 分钟）

1. 更新 [11 MVP 边界](11-MVP边界.md)：完成的移出范围表，新想法进 Backlog
2. 更新本文档勾选状态
3. 检查"可讲的数据"一栏——**如果为空，说明这一周走偏了**
4. 只允许从 Backlog 往上解锁，**不允许往 MVP 里塞新东西**

---

## 并行策略

**整块交给 coding agent**：前端全部页面与组件、解析流水线、Docker 与部署配置、
入库脚本、评测跑批的工程骨架、看板可视化、测试用例、安全加固的具体实现。

**自己想清楚再让 agent 落地**（面试主战场）：
1. 检索融合策略与参数
2. Agent 图结构与工具描述
3. gold_spans 映射规则与评测指标定义
4. 门禁阈值与噪声控制
5. 模型路由分档与成本口径

---

## 绝不能砍的五项

评测集与 Judge 校准 · CI 门禁 · 断点续跑与副作用幂等 · 引用溯源 · 拒答

砍掉任何一项，项目就退化成"又一个 RAG 聊天机器人"。

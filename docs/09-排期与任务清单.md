# 09 · 排期与任务清单

**起始：2026-08-13（周四）｜结束：2026-09-23**

> **范围以 [11 MVP 边界](11-MVP边界.md) 为准。** 本文档是它的逐周展开。
> 蓝图里但不在本排期的功能，在 [11 §5 Backlog](11-MVP边界.md) 排队，不是被删除。

秋招提前批已在进行，正式批 8–9 月。M0 先锁定本地可验收闭环与基线。

> ## ⚠️ 2026-08-22：这份排期的后半段已经被形态转变作废
>
> W1–W5 那些 `[x]` 是**历史台账**，记录当时真的做了什么，照原样保留——包括那些现在已经
> 不成立的选型（Postgres/Redis/Arq/pgvector/Langfuse）。**不要照着它们重做。**
>
> 作废的是 W6 的「最终公网部署」整块，以及所有依赖服务端形态的收尾项：产品转成桌面应用
> 之后，公网 demo 做不到也不该做到（[02 §5](02-架构设计.md)）。
>
> **当前真正的待办已经不在这份文档里**，按优先级是：
>
> 1. **冻结新的评测基线**——Cowork runner、文件系统 KB 稳定锚点和阅读三指标均已恢复；
>    还需 owner 复核 26 条检索候选集、补真实问法/OCR/表格/multi-hop，并在此之后替换旧快照
>    （[06 §4.5](06-评测体系.md)）。
> 2. **决定拿什么替代公网 demo**（[02 §5](02-架构设计.md) 三选一，尚未决定）。
> 3. 摘掉约 190 处惰性 `session` 形参（[ADR-0012](adr/0012-退役postgres与redis改用本机文件.md)）。
> 4. 补回请求层与会话层限流——只在要上公网时才是阻塞项（[12 §2.2](12-安全与部署.md)）。

> **进度快照（2026-08-17）**：M0 已收口；W3 检索升级、W4 固定综述 Agent 和 W5 三档路由
> 的工程实现均已提前完成。80 条 human 保持 70 dev + 10 隔离 test，test 仍未访问；
> evidence gate 修复后不可答 13/13，可答题实际回答 44/57。实验 C、E8、E9、G0、J1 已收口，
> J2 又以 19 条独立盲标完成六类 binary correctness 校准：heavy 18/19、main 19/19，均过门。
> 成本看板与 PR 静态 CI 已落地。当前进入**功能冻结**，只做夜间 baseline、
> 唯一一次 core-test、文档/前端打磨、安全与公网交付，不再解锁新功能。

| 里程碑 | 区间 | 交付 | 硬门槛 |
|---|---|---|---|
| M0 | 08-13 ～ 08-26 | 入库 + dense 检索 + 溯源 + 拒答 + 安全边界 + 基线 | 本地完整闭环 + 基线数字 |
| M1 | 08-27 ～ 09-09 | 混合检索 + rerank + 固定综述工作流 + Judge 校准 | 第一批真实增量数字 |
| M2 | 09-10 ～ 09-23 | 三档路由 + CI 门禁 + 输出 | 台账有厚度，含负优化 |

**贯穿全程**：从 W1 起自己每天用。dogfooding 天数是成功标准之一，也是 badcase 唯一来源。

---

## W1｜08-13 ～ 08-19：入库链路打通

> 本周纪律：**评测标注同步启动，不能等系统做完再标**（R3）。

### 环境（第 1 天做完）
- [x] 安装 OrbStack（Docker 29.4.0 已验证）
- [x] `uv python pin 3.12`（当前锁定 3.12.13）
- [x] `deploy/docker-compose.yml`：postgres+pgvector、redis（**minio/langfuse 推到 M1**）
- [x] 初始化 SQL：[03](03-数据模型.md) 的知识层 + 评测层 + `llm_calls` +
      `agent_runs` / `run_events`（SSE 基础）；**其余 Agent 四表推到 M1**；启用 `vector`
      （`zhparser` 随 M1 词法检索引入）
- [x] 后端骨架：FastAPI + Pydantic Settings + structlog(trace_id) + 健康检查

### 模型网关（先于业务代码，否则后期改不动）
- [x] `llm/gateway.py`：`complete()` / `stream()` / `embed()` 统一入口
- [x] `llm/routing.py` + `config/routing.yaml`：M0 的统一入口保留，W5 已扩成
      `light/main/heavy/external` 四档、fallback 链与加载期校验
- [x] `llm/budget.py`：**调用前**原子预留、调用后结算（[12 §2.2](12-安全与部署.md)）；
      释放/保守记账/到期 sweeper 三条路径分开
- [x] 每次调用写 `llm_calls`

### 入库链路 ★ 本周主线
- [x] `sources` + `local_dir` connector（含 realpath 校验、持久化增量游标、文件级失败隔离）
- [x] **PDF 解析**（MinerU）：双栏重排序、表格转 Markdown、公式转 LaTeX、页眉页脚剔除
  - [x] PyMuPDF 文本层基线：基础双栏排序、重复页眉/页脚过滤、页码与 bbox 定位
  - [x] auto 路由、结构/文本质量门控、超时清理、显式回退与解析元数据
  - [x] 真实 PDF 跑批 + bbox 抽样叠图；跨页表格/扫描件专项样本继续补充
- [x] **Markdown 解析**
- [x] 输出 `document_versions.full_text` + `parsed_blocks`
      （NFC Unicode code-point `char_start/end`）+ `parsed_block_locations`
      （`bbox_norm`、`page_width/height`、`rotation`、`coord_origin`）
- [x] 候选版本完整成功后再事务激活；验证新版解析失败时旧版仍可检索，
      且慢到的旧候选不能覆盖已激活的新候选
- [x] **PDF 解析跑在独立子进程**，带文件大小、页数、内存/CPU/超时上限
- [x] `heading` 分块（**只做这一种**）→ `chunks`，记录 block 区间与字符区间
- [x] bge-m3 dense embedding：1024 维、模型/provider/revision 身份隔离，维度与写入前校验
- [ ] bge-m3 sparse 输出存储：当前 OpenAI-compatible embedding 契约只返回 dense；
      sparse 第三路检索仍按 [11 Backlog #6](11-MVP边界.md) 管理，不属于已实现能力
- [x] dense 检索 + 生成 + 引用标记解析（引用锚 `block_id`）
- [x] **每策略一个 HNSW 部分索引** + `hnsw.iterative_scan`（谓词含 `is_searchable`；
      批量灌几万 chunk 时先 DROP 再重建更快）

### 语料与标注
- [ ] 导入 ≥ 200 篇真实资料，抽样 20 条人工核验解析质量
      —— **推迟（2026-08-14 决定）**：当前 40 篇已够跑通链路与出正式基线，
      扩容留到检索实验需要更大语料时再做。注意扩容会让 M0 基线数字失效，
      必须在同一份 suite 上重跑才能与旧数字对比
- [x] 定 `gold_spans` 标注规范 + 本地标注工具（UTF-16 选区由后端转 code-point，入库 quote 校验；PDF bbox 预览）
- [x] **标注 20 条**（覆盖 single_hop / multi_hop / table / unanswerable）

**W1 交付**：命令行/API 能问能答带引用 ｜ 20 条标注

---

## W2｜08-20 ～ 08-26：M0 闭环 ★ 硬门槛

### 流式与恢复（[ADR-0007](adr/0007-agent幂等与事件溯源.md)）
- [x] `agent_runs` + `run_events` 两表（普通对话也走 run）
- [x] Arq worker：任务**不依附 HTTP 连接**
- [x] `POST /runs` → `GET /runs/{id}/events?after_seq=N`（先补历史再续流）
- [x] run worker 租约 + heartbeat + watchdog；普通生成崩溃显式失败，不静默重复计费
- [x] SSE 信封 `{id: "run:seq", run_id, seq, type, data}`，`id` 用作 `Last-Event-ID`
- [x] `messages.status` = streaming/completed/failed/cancelled
- [x] **真流式生成**：`gateway.stream()` 逐 token 产出（此前是整答生成完再切片）。
      判定链（检索 → 阈值 → 证据门控）全部在生成之前跑完，因此拒答零 delta；
      流式与非流式共用同一个生成器，评测与线上不会分叉（约束 6）。
      实测首字 2.7s、之后每 50–70ms 一段；改前是 3.3s 一次性全出

### 前端（阶段 1–2）
- [x] Next.js + 原生 CSS + 自写 SSE 客户端（没有为 M0 强行引入 shadcn / Vercel AI SDK）
- [x] 对话流：流式消息 + 引用卡片
- [x] 右栏原文预览：按 `bbox_norm` × 实际渲染尺寸定位高亮
- [x] 资料库页：列表 + 导入/解析状态。状态按版本激活规则分四档
      （可检索 / 解析中 / **新版失败但旧版仍在服务** / 无可检索块），
      失败原因与"能不能高亮"逐行可见；写侧仍强制 admin
- [x] 回答正文 Markdown 渲染：分块增量解析（不是每个 delta 全量重解析）、
      `[S1]` 渲染成可点锚点并复用引用选中态、不渲染裸 HTML 与 `javascript:` 链接
- [x] **边界情况 B1 / B2 / B3 自测通过**（Playwright 生产构建验收）

### 拒答
- [x] 检索分数阈值拒答（阈值先拍，M1 做 ROC）
- [x] 前端体面拒答：拒答卡片上一键切"基于通用知识回答"。
      模式落在 `agent_runs.answer_mode`（不是队列参数，重投递也不会串），
      该模式跳过检索、不产引用，`message.done` 带 `grounded=false`，前端挂醒目免责条

### 安全 ★ 上线前置（[12](12-安全与部署.md)）
- [x] demo admin 登录 + demo 只读 session；对话/run/SSE 按 session 做对象级鉴权
      （私人语料只存在本地 owner 环境）
- [x] 语料维护、同步、直接检索和标注 API 强制 admin；citation 原文按 session/run 授权
- [x] Redis per-IP token bucket（20/min、burst 5）+ PostgreSQL per-session 20 次提问配额
- [x] 每日费用调用前原子预留与硬上限
- [ ] 上传：MIME 嗅探白名单、50MB、500 页、120s 超时
      —— **推迟**：本地 owner 环境只走 `local_dir` 同步，没有开放上传入口；
      这条是上线前置，跟着部署一起做
- [x] 路径穿越防护；`web_clip` 尚未实现，因此没有开放 SSRF 入口
- [ ] **私人语料与演示语料物理隔离**（独立 PG 实例，无同步路径）
      —— **推迟**：只在公网暴露时才成立的约束，跟着 W6 的部署一起做
- [ ] 走完 [12 §5 上线检查清单](12-安全与部署.md)，逐条打勾
      —— **推迟**：同上，上线那一步再逐条打勾

> **2026-08-14 排期决定**：语料扩容与全部上线相关项一并后移，**不再阻塞 M0 收口**。
> M0 的完成判据因此收敛为「本地闭环可验收 + 正式基线数字入台账」，
> 已经达成的鉴权、限流、费用闸门保持有效，不因后移而回退。

### 评测基线
- [x] 标注补到 **40 条**（core-dev 20 + english-dev 20，owner 已确认逐条复核）
- [x] `gold_spans` → chunk 的区间映射（θ=0.5）+ quote/stale 校验
- [x] dense 规则轨指标：span-level Recall@k / budget Recall / nDCG@k / α-nDCG@k / MRR / context precision / 拒答检出
- [x] 生成规则轨补充 `citation_validity` 与答案约束检查
- [x] 人工检查正式生成的 44 个引用是否真正支撑答案：42/44，准确率 95.45%
- [x] `eval/dense_baseline.py` 跑批骨架（DB 结果、模型身份/config hash、fallback=false）
- [x] 20 条人工集首版跑批 + 分类指标 / Top-50 漏召回诊断 / 拒答分布入台账
- [x] 40 条人工集自动轨跑批 → 检索、拒答、引用规则数字入台账
- [x] 填完 44 个实际引用的人工语义支撑复核，并回填 `citation_accuracy` 与 DB human_label

### M0 收口
- [x] README：项目定位、真实实现状态、指标边界与长期蓝图
- [ ] README：架构图、完整本地使用说明与一条命令启动（留到 W6 对外交付）
- [ ] 公网部署前置项只记录不执行；实际部署统一移到 W6 最后一项

**W2 交付**：本地可验收 demo ｜ 安全边界 ｜ 正式基线数字
**可讲的数据**（[M0 台账](experiments/2026-08-14-M0-40条正式基线.md)）：
span Recall@10 68.23% ｜ nDCG@10 46.69% ｜ 人工引用准确率 95.45%（42/44）｜
不可答正确拒答率 100%（8/8）但可答题误拒 16/32，端到端拒答准确率 60%。
**最有讲头的一条**：`citation_validity` 100% 而人工语义支撑 95.45%——
规则轨查不出"引用真实但不支撑论断"，这正是 M1 要上校准 Judge 的理由。

---

## W3｜08-27 ～ 09-02：混合检索 + 四策略对照

### 检索升级
- [x] 分语言 PG `tsvector` + `ts_rank` 词法检索（实验 E2 确认英文收益）
- [x] RRF 融合
- [x] rerank（bge-reranker-v2-m3，本地服务，失败回退原排序）
- [x] 查询分解 + 多查询扩展（默认关闭，已有单变量实验）
- [x] `hnsw.iterative_scan` 配置与过滤条件覆盖测试

### 四策略对照 ★ 第一个正式实验
- [x] 建 `fixed` / `recursive` / `semantic` 三套 chunk（`heading` 已有）：40 个 active version
      全部构建成功；当前可搜索数量 fixed 1503 / heading 1862 / recursive 1531 / semantic 2808，
      embedding 身份一致且重复执行幂等
- [x] **每策略一个部分 HNSW 索引**（[03 §4.1](03-数据模型.md)）——索引与扫描参数已就位,
      四套正式数据已灌入；查询显式命中对应 strategy 的部分索引，隔离与计划测试已覆盖
- [x] E1 检索轨首批：`core-dev/human` 20 条使用同一份 `gold_spans`、同一检索配置跑四策略，
      四个 run 均为 20/20 完成、0 error、fallback=false，代码提交 `7d7b9bb`
- [x] E1 主检索口径使用固定 4000-token budget，同时报告 span recall / nDCG / MRR /
      context precision / 冗余率 / Top-K 上下文 token / 延迟，并完成 10,000 次配对 bootstrap
- [x] E1 完整人工集检索收口：`english-dev/human` 20 条四策略均 20/20、0 error，
      与 core-dev 分层 bootstrap；两层不混合统计，报告 `c7f25da`
- [x] E1 生成轨首批：`core-dev/human` 四策略正式生成均 20/20、0 error，已补
      constraint / citation / 端到端延迟 / token；金额成本无有效记录，明确标为不可用
- [x] E1 English 生成外推检查已纳入 70 条 dev heading 扩展跑批：`english-dev` 20/20，
      全套 70/70、0 error；结论不再只限于 core-dev 20 条
- [x] 混合检索增益、rerank 与查询改写已有单变量实验和负结果记录
      （[D2](experiments/2026-08-14-D2-词法检索与RRF单变量对照.md)、
      [D3](experiments/2026-08-14-D3-多查询精排与组合拒答.md)、
      [D4](experiments/2026-08-14-D4-本地cross-encoder精排.md)）
- [x] 补 E1 正式台账：记录实验前 heading 预判、中英文分层检索、core-dev 生成轨、
      异常样本与结论边界；阶段决策为保留 heading 默认、semantic 作为效率候选

### 评测扩充
- [x] 基线更新为 **80 条 human**，dev/test 拆分为 70 / 10
  - [x] 隔离式候选构建、七类/双语精确配额、document-level test 切分、稳定指纹、
        Unicode gold span 与跨 split 泄漏审计已实现
  - [x] 首版 80 条证据草稿已跑独立审计：86/86 span 有效，但内容门禁拒绝
        54 条通用模板、5 条占位和 58 条 raw-block answer；`--apply` 拒绝且 DB 未写入
  - [x] 放弃修补自动草稿；新增 40 条人工撰写题（dev 30 + test 10），内容审计通过并经
        作者逐条复核后显式 promotion 为 human
  - [x] 新增题与旧 40 条合并为 70 dev + 10 test；test 与 dev 按 document version 隔离
  - [x] 当前覆盖六类；`agent_task` 在真实工具和执行产物闭环前保持 0，不用占位题凑数
- [x] `eval/compare.py` 两次跑批严格配对 diff；`eval/strategy_matrix.py` 支持 N 策略矩阵
- [x] paired bootstrap 置信区间（固定 seed/resamples，替代"三次取中位数"）
- [x] `chunk_strategy_runner` manifest 可直接输入四策略矩阵；fail-closed 校验 dataset、
      item、gold span 指纹、失败样本与受控配置差异
- [x] 70 条 dev 扩展检索收口：只比较 heading / semantic 两个 finalist，严格配对 70 条，
      test 访问为 0；质量差异 CI 均跨 0，semantic 显著减少 707.5 token/条和 269.2ms/条
- [x] 70 条 dev heading 生成收口：70/70、0 error；unanswerable 13/13 正确拒答，
      answerable 仅回答 36/57，确认 evidence gate 过度拒答为下一轮主 badcase
- [x] [E4 扩展评测台账](experiments/2026-08-16-E4-70条dev扩展评测收口.md)：
      固化 suite SHA、授权范围、检索矩阵、生成指标与 Judge 包追溯路径
- [x] 21 条 answerable 误拒归因与 evidence gate 修复：旧 round-robin 打包改为保持 rerank
      顺序，非法结构只重试一次且继续 fail-closed；同套件复跑恢复 10 条、回归 2 条，净恢复 8 条
- [x] [E5 gate 修复台账](experiments/2026-08-16-E5-evidence-gate误拒修复.md)：
      剩余 13 条拆为 11 条 retrieval miss、1 条 12k evidence budget miss、1 条标注缺陷

**W3 当前交付（2026-08-16）**：混合检索上线 ｜ 四策略工程闭环 ｜ 80 条 human
（70 dev + 10 test）｜ 70 条 dev finalist 检索 ｜ 70 条 heading 生成 ｜ 六类 Judge 包 ｜
[E1 正式台账](experiments/2026-08-16-E1-分块策略四方案正式对照.md) ｜
[E4 扩展收口](experiments/2026-08-16-E4-70条dev扩展评测收口.md) ｜
[E5 gate 修复](experiments/2026-08-16-E5-evidence-gate误拒修复.md)
**W3 剩余硬门槛**：无；10 条 test 继续隔离。
**当前可讲的数据**：heading 的检索质量点估计最稳健但未显著胜出；semantic 在 70 条上
显著减少 707.5 个 Top-K token 和 269.2ms 延迟。heading 生成的不可答拒答为 13/13，
gate 修复后不可答仍为 13/13，可答题实际回答提升到 44/57；下一步不再盲调 gate，优先处理
11 条 retrieval miss，并单独验证 1 条多跳证据预算缺失。

---

## W4｜09-03 ～ 09-09：固定综述工作流 + Judge 校准

### Agent（**固定流程，不是通用 Agent**）
- [x] 补齐 Agent 四表：`agent_plan_steps` / `agent_attempts` / `tool_invocations` / `agent_checkpoints`
- [x] 确定性固定流程：筛选→抽卡→分组→对比→生成→写回
- [x] `AgentState` 可序列化 + Postgres checkpointer
- [x] 固定图工具：`list_documents` / `extract_card` / `group_cards` / `compare_docs` /
      `generate_review` / `write_note`；`search_knowledge` 留给下一版检索增强
- [x] **面向模型的错误信息**（约束 4）：schema、证据逐字校验、路径与步骤错误均返回可修复信息
- [x] **副作用幂等协议**（[03 §6.3](03-数据模型.md)）：`write_note` 已走 `tool_invocations`
- [x] 幂等租约回收：过期 `in_flight` / `failed` 可 CAS 重试，原子 rename 后崩溃不重复写
- [x] HITL：`write_note` 强制确认；已验证拒绝零写入、重复确认和 interrupt 恢复不重复写入
- [x] 预算熔断（token / 调用数 / 墙钟）：调用数与墙钟调用前精确判定，token 按最坏情况
      预留、按实际结算；墙钟只累计执行分段，不计 `waiting_human`；熔断落终态且不重试
      （口径见 [05 §预算熔断的四条口径](05-Agent设计.md)）
- [x] checkpoint 恢复集成测试：新执行器从最近 cursor 继续，已完成只读节点不重跑
- [x] 杀真实 worker 进程并录制 checkpoint 恢复 demo：`SIGKILL` 后 watchdog 将带 checkpoint
      的固定综述退回队列，新 worker 从 compare 继续，已完成节点重跑 0 次
- [x] 前端事件状态层已消费 `plan` / `step.update` / `interrupt` / `artifact` / `run.done`
- [x] 前端固定综述创建页、时间线 UI 与批准/拒绝控件；专项 Playwright 7/7、全量 30/30，
      覆盖确认前零写入、拒绝、批准、刷新续流和 worker 恢复提示
- [x] 前端 admin 登录入口：顶栏浮层登录/登出，未登录时写操作不留假可点，`401` 就地拉回
      未登录，`503` 给的是配置指引而不是"密码错误"；假后端补齐 admin 门后全量 37/37
- [ ] 边界情况 B4–B10 自测（B4 的 Markdown 增量解析已在 M0 提前做掉，
      只剩超长回答的虚拟滚动）

### Judge 校准 ★ 评测可信度的地基
- [x] 六类 binary correctness 中间校准：作者独立盲标 validation 19 条；heavy
      accuracy/QWK = 0.9474/0.8725，main = 1.0/1.0，两档均通过 0.85 门槛；
      10 条 test 未参与校准，类别切片因每类仅 2–5 条只作诊断
- [x] Judge 校准工程：唯一 case 导出、人工标签模板、固定 rubric/prompt 指纹、
      calibration/validation 稳定拆分、版本化 DB namespace 与幂等/漂移门禁
- [x] 准确率 + **quadratic weighted Kappa** + 固定 `[0,1]` 混淆矩阵/边际/
      类别切片/bootstrap 已实现；70/70、6/6 类，calibration / validation 稳定拆分 51 / 19；
      小样本 bootstrap 的单一标签退化改为确定性补抽并保留丢弃审计
- [x] `heavy` 端点准备：DeepSeek `deepseek-v4-flash` 的 exact-model 与合成 chat smoke 已通过，
      health report 明确记录未发送项目数据
- [x] `heavy` 档正式跑批与版本化 DB 写回：70/70，fallback=false；独立标签完成后
      写入 `judge_calibration/answer-correctness-binary.v2/answer_correctness` namespace
- [x] 首轮 frozen rubric 已达标，不依据 validation 分歧反向改 rubric；唯一 heavy 分歧为
      global 类“核心概括是否覆盖多个来源视角”，保留为边界案例
- [x] Judge runner 默认拒绝发送并禁用 fallback；正式调用逐条记录 actual model、raw、
      reason、token 与授权说明指纹，身份漂移立即失败
- [x] 固定 Agent 闭环后启动 `agent_task`：首个种子来自真实 6 步成功 run，带 HITL、
      非空 gold tools、幂等记录与产物 SHA，保持 `pending_human_review`
- [ ] 扩充并人工复核 `agent_task`，冻结规则轨后再升级为七类正式校准

### Agent 实验
- [x] A1 面向模型的错误信息：20 篇真实文档上首轮失败集合一致；具体可执行错误恢复 2/14，
      通用错误恢复 0/14，但首轮失败率仍为 70%，结论是有效但不是主修复
- [x] A2 工具描述样例：固定图没有工具选择事件，改测结构化抽取首轮合格率；填充样例
      30% → 30%，加入失败样例降至 20%，并暴露示例内容污染，决定不改生产 prompt
- [x] A3 证据编号锚定：模型选 E 编号、服务端回填原文切片；20 篇首轮 15/20，线上同构
      两轮修复后 20/20，全部 quote 可回到模型可见正文精确区间；已生成 1 条真实 agent_task
      种子和九项规则轨；A6 语义评测收口后当前全量后端测试 490/490
- [x] A5 工程闭环：owner-only 两阶段异步抽取、四操作时序更新、置顶保护、回答前 top-5
      召回、字符预算与 `/memory` 当前/历史管理页；匿名 demo 的抽取、召回和管理负向测试已固化。
      全量后端 481/481、Playwright 41/41，Ruff、mypy、ESLint 和 TypeScript 全绿。
      获 owner 授权后已在 `qwen3.6-35b-a3b` 完成 10 条 synthetic paired smoke：
      词面规则轨 0/10 → 9/10，Δ=+0.90，95% CI [+0.70,+1.00]。该结果只证明工程链路；
      owner 盲评 10/10 已完成，平均分 2.2 → 4.0，Δ=+1.8，95% CI [-0.1,+3.4]，
      偏好票 3:7；方向正向但区间跨 0，不把 synthetic 结果冒充 human 产品结论
- [x] A6 独立语义回归：先冻结 12 条全新 synthetic suite、三维 rubric 与六项门槛，
      再用 `qwen3.6-35b-a3b` 生成 24 个配对回答、`deepseek-v4-flash` 匿名评分。
      memory use 0.83 → 1.83，qualified 2/12 → 7/12，偏好票 2:8（另 2 tie），来源安全
      12/12；但两题 task quality 退化且 qualified 未到 9/12，预注册总门槛失败，不宣称达标

**W4 当前交付**：综述工作流可用且可靠 ｜ 六类 binary correctness Judge 校准通过
**可讲的数据**：Kappa 值、恢复率、幂等验证结果

---

## W5｜09-10 ～ 09-16：三档路由 + CI 门禁

> **M2 已选择选项 B（三档路由）**，理由与冻结边界见 [11 §4](11-MVP边界.md)。
> 长期记忆不再与本周并行，保持在 Backlog #1，最终交付完成前不解锁。

### 三档模型路由
- [x] 路由表补齐 `light` / `main` / `heavy` + `external` 四档与 fallback 链：
      `config/routing.yaml` 入 git（凭据走 `${NAME}` 展开），加载期校验档位名、
      自引用、未知变量与未声明档位；未部署的档位线上沿链降级并在启动告警，
      评测模式直接失败。`llm_calls.tier` 记实际作答档位，fallback 两次尝试各记一条账。
      **light 与 external 尚未接入线上路由**，所以当前业务调用实际落在 main；
      日常 binary correctness Judge 也采用 main，heavy 只保留里程碑复核
- [x] 置信度驱动的动态升档（结构化抽取）：`app/llm/escalation.py`，只升一档、
      升档轮从干净上下文重来、判定全是免费确定性检查；起始档与目标档都由
      `routing.yaml` 决定。`evidence_sufficiency` / `agent_extract_card` 已登记
      升档目标但**未启用**（起始档仍是 main），等对照数据再翻
- [x] 精确缓存：Redis + 24h TTL，键含档位/模型/provider/max_tokens/请求指纹；
      采样、评测模式、失败结果一律不缓存，Redis 异常降级成未命中。命中写
      `llm_calls.cached` + `cache_type='exact'` 且成本为 0，看板可直接算命中率。
      **流式刻意未接**（与取消、预算结算、吐字后不 fallback 三件事耦合）
- [x] Prompt 缓存前提固化：系统提示为稳定前缀，用例钉住"两次不同输入的 system 段
      逐字相同"与"系统提示里无动态内容标记"
- [ ] 语义缓存 —— **留在 Backlog**，不在本周范围
- [x] **实验 C：帕累托前沿**：矩阵按真实链路重做为 2 因子（生成 × 证据门控），
      7 配置 × 3 重复；C8 因 external 未部署跳过。台账见
      [experiments/2026-08-16-C](experiments/2026-08-16-C-质量成本帕累托前沿.md)。
      关键结论：light 首轮的"容量边界"实为**部署参数**（原生 262144 被压到 8192），
      修正 vLLM 启动参数后零错误、吞吐 4.8×；heavy 在 temperature=0 下三次重复跨度
      达 13.5pp，不可复现；**heavy 用在证据门控上是净负面**（对齐 14.7%，全场最低）；
      两个质量轴给出相反排序——**C3 全轻档在引用对齐上超过线上基准 C2 且快 16%**，
      但约束通过率低 10pp
- [x] **实验 J：Judge 降档**（`main` 能否替代 `heavy`）——走口径 B（判者间一致性），
      因为口径 A 当前无分辨空间：唯一一份标签是助手起草的，heavy 在其上已 1.0000。
      结果：一致率 **0.9714** / QWK **0.9191**（分歧 2/70），输出 token 省 **2.8×**、
      墙钟省 **8.3×**；heavy 两次重复判决逐条相同——**G0 推测的"短离散输出可复现"成立**。
      J2 的独立 validation 又得到 main 19/19、heavy 18/19；正式采纳 main 作为日常
      correctness Judge（必须关思考链，只覆盖 binary rubric）。台账见
      [J1](experiments/2026-08-17-J1-Judge降档判者间一致性.md) 与
      [J2](experiments/2026-08-17-J2-独立盲标与Judge正式校准.md)
- [x] 成本口径按 [07 §7](07-模型路由与成本.md)：新增 `gpu_batches` 表与
      `app/llm/batch.py` 批次上下文，`app/services/cost_report.py` 按整批 GPU wall time
      摊销并同时报每任务/每 ktok 成本、吞吐、并发度、客户端占用率。
      墙钟由进程侧单调时钟测量（`now()` 是事务开始时间，从 `created_at` 反推恒为 0）；
      缓存命中不进分母；未收尾批次整批排除；没配单价与来源直接硬失败。
      **GPU SM 利用率仍未采集**（需推理机侧 nvidia-smi），报告里如实标注为占用率
- [x] 成本看板：admin-only `/api/v1/cost/overview` + `/cost` 页面；按档位/任务类型聚合调用、
      token、缓存、失败与 fallback，并把批次成本与吞吐、并发度、客户端占用率同时展示；
      缺价格保持 `null/不可用`，不伪装成 `$0.00`，后端专项测试 10 条

### CI 门禁
- [x] 静态检查范围已冻结：Ruff 覆盖 `backend/app`、`backend/tests`、
      `backend/migrations`、`eval`；mypy 覆盖 `backend/app`；前端执行 ESLint + TypeScript
      typecheck。Python Ruff 必须显式使用 `backend/pyproject.toml`
- [x] 静态轨 workflow 落地：`.github/workflows/ci.yml`，backend（pgvector + redis
      service，`uv sync --locked` → ruff → mypy → pytest）与 frontend（`npm ci` →
      eslint → tsc）两个 job。用干净 worktree（无 gitignore 的根 `.env`）验证过，
      并因此抓到两条只在 CI 上才炸的问题：pytest 需要真 Redis；
      `tsc` 依赖 Next 生成的 `LayoutProps`，已把 `next typegen` 并进 `npm run typecheck`。
      台账见 [G0](experiments/2026-08-17-G0-门禁准备与噪声地板.md)
- [x] ~~**PR：20–30 条确定性 smoke**~~ —— **已取消，分层从三层收成两层**。
      合成 fixture 上的逐位相等快照本质是 golden-file 测试，而 `backend/tests/`
      已覆盖规则轨全部确定性路径（端到端 `test_retrieval_generation_and_block_citation_chain`、
      RRF、词法三模式、证据打包、引用 fail-closed、两条拒答路径、HNSW 等价），
      且每个 PR 都在跑；合成语料上的指标又不构成质量信号。
      连带取消：公开 fixture 语料、预计算 embedding 入 git、冻结 UUID 的 DB seed，
      与约束 7 的张力随之消失。理由与代价见
      [06 §4.1](06-评测体系.md) 与 [G0](experiments/2026-08-17-G0-门禁准备与噪声地板.md)
- [x] 门禁阈值：噪声已用实验 C 的 21 次跑批重算（G0），阈值表按实测修订完毕
      （[06 §4.2](06-评测体系.md)）。要点：`main` 档生成三次重复**逐位一致**，
      所以规则轨指标要求**聚合值零回退**而不是百分比余量；成本 ≤20% 站得住
      （噪声最坏 8.8%）；**延迟移出门禁**（同配置相对跨度 75.8% ≫ 原定 30%）；
      Judge 打分的三个指标等实验 J 收口后再启用
- [x] `eval/gate.py` —— **夜间层**，跑在本机/集群。`snapshot` 导出可提交的 baseline
      快照（字段白名单，只留数字与 UUID，gold span 存 sha256 指纹，不触碰约束 7），
      `check` 复用 `compare.py` 的配对与兼容性校验后判定。规则：规则轨**聚合值**零回退、
      成本 token 口径 ≤ +20%、受门禁指标无可配对样本即判不合格；拒判（退出码 2）
      与判为不合格（退出码 1）分开。19 条 fixture 测试，已用 E1 的
      heading / semantic 两份真实报告验过通过与阻断两条路径
- [x] badcase 回流的**棘轮**落地：`backend/tests/test_regression_badcases.py` 的
      `BADCASES` 登记表 + 元测试，covering 用例被改名或删掉直接红。
      已登记四条真实修复：E2 `ts_rank_cd` 中文 bigram、E5 evidence gate 误拒、
      C 成本查询按 label 撞车、G1 门禁拒判退出码撞车
- [ ] **点踩采集入口**：`feedback` 表建了但 `backend/app/` 里没有任何代码写它，
      没有接口也没有前端入口。回流链现在从"作者自己发现 badcase"开始跑，
      不是从点踩开始。属产品功能，此前排期里没有位置，先记在这里
- [x] 首份 baseline 快照，**两条轨都已提交**：`eval/snapshots/retrieval.json` 与
      `eval/snapshots/generation.json`，均用 HEAD 上生产配置的正式跑批导出、0 失败样本。
      检索轨指标与 E9 逐位复现；生成轨已取得 owner 的模型外发授权，授权说明与指纹写进
      manifest（[G1 台账](experiments/2026-08-17-G1-首份baseline快照与门禁点亮.md)）。
      快照按报告类型自动解析，`--baseline` 仍可覆盖。
      点亮过程中修掉一个真 bug：比较层的 `ValueError` 漏成 traceback + 退出码 1，
      把"跑批配错了"误报成"质量回退"；并补上生成轨缺失的子报告合并步骤
- [ ] **夜间：完整 dev 集 + Judge**（在本地/集群跑，结果回传）。
      规则轨两条已就位；`answer_correctness` 接进 generation 快照字段后才能解除 pending
- [ ] 生成轨阻断路径待真实回退跑批出现时补验（当前只有 fixture 覆盖）
- [ ] 把 gate 真正挂进夜间定时任务（当前仍是手工跑）

### 可观测
- [x] ~~Langfuse 接入~~ → **不做了**。桌面产品把 trace 发到第三方是一件需要单独征得同意
      的事，不该顺手做；现在只有结构化日志的 `trace_id` 与 `telemetry.db` 的 `llm_calls`
      （[12 §3.3](12-安全与部署.md)）

**W5 当前交付**：三档路由、成本看板、PR 静态 CI 已完成；夜间 gate 已在检索轨与生成轨
双双点亮（两份 baseline 均已提交）；缺的是 Judge 指标接入快照与夜间定时化
**可讲的数据**：成本下降 %、帕累托曲线、Judge 降档结论、门禁拦截案例

---

## W6｜09-17 ～ 09-23：收口与输出

### 功能收尾
- [x] 拒答阈值 ROC 调优 / HNSW 参数 / top-k —— **三项全部收口，结论都是不调**：
      - 拒答阈值维持 `0.35`（[E8](experiments/2026-08-17-E8-拒答阈值ROC调优.md)）。
        只扫分数门会建议提到 0.60，但组合门下分数门在 [0.20, 0.5750] 完全惰性、
        误答恒为 0（证据门控已拦下 13/13 不可答），提阈值只增误拒
      - HNSW 参数维持（[E9](experiments/2026-08-17-E9-HNSW参数与topk收口.md)）。
        `ef_search` 拉 8× 指标逐位不变——EXPLAIN 显示当前规模下规划器走顺序扫描，
        向量索引根本没被用上，GUC 是空操作。**语料涨上去后必须重跑**
      - `top_k=10` 维持。瓶颈是单文档霸榜（Top-K 最大单文档占比均值 75.86%，
        25/70 条题目被单一文档 100% 独占），不是候选深度
- [ ] 前端整体打磨
- [ ] `core-test` 集**首次也是唯一一次**跑批 → 最终数字
- [x] 功能冻结：在 Judge 校准、nightly baseline、core-test 和最终交付完成前不解锁 Backlog

### 对外输出（别拖到最后一天）
- [ ] README：架构图、指标表、一条命令启动（**现在真的是一条**：`uv run uvicorn`，没有容器）
- [ ] 博客 1：《混合检索到底提升了多少：一份可复现的实验记录》
- [ ] 博客 2：《给 Agent 建一道 CI 门禁》
- [ ] Demo 视频 3 分钟，**必含两个镜头**：断点续跑、正确拒答
- [ ] 简历按 [10](10-简历与面试.md) 模板填真实数字
- [ ] 对照 [10 §3](10-简历与面试.md) 的必答题自查

### ~~最终公网部署（全项目最后一步）~~ → **整块作废**

产品转成桌面应用，全部价值在于它读用户**自己机器上**的文件，这件事公网 demo 做不到。
替代方案三选一（录屏 + 安装包 / 公开语料只读实例 / 只做可复现评测跑批）与各自代价见
[02 §5](02-架构设计.md)，**尚未决定**——这是当前最需要拿主意的一件事。

真要上公网，先补 [12 §5](12-安全与部署.md) 那份清单里勾不上的三条（per-IP 限流、
per-session 配额、demo admin 隔离）。

取而代之的桌面发布待办：
- [ ] 代码签名与公证（macOS Gatekeeper）
- [ ] 自动更新通道
- [ ] 首次运行的 `~/.workpilot` 目录权限（0700）验收

**W6 交付**：完整开源项目 + 2 篇博客 + 1 个视频 + 简历定稿

---

## 每周日：范围复核（15 分钟）

1. 更新 [11 MVP 边界](11-MVP边界.md)：完成的移出范围表，新想法进 Backlog
2. 更新本文档勾选状态
3. 检查"可讲的数据"一栏——**如果为空，说明这一周走偏了**
4. 只允许从 Backlog 往上解锁，**不允许往 MVP 里塞新东西**

---

## 并行策略

**整块交给 coding agent**：前端全部页面与组件、解析流水线、Docker 与部署配置、
入库脚本、评测跑批的工程骨架、看板可视化、测试用例、安全加固的具体实现。

**自己想清楚再让 agent 落地**（面试主战场）：
1. 检索融合策略与参数
2. Agent 图结构与工具描述
3. gold_spans 映射规则与评测指标定义
4. 门禁阈值与噪声控制
5. 模型路由分档与成本口径

---

## 绝不能砍的五项

评测集与 Judge 校准 · CI 门禁 · 断点续跑与副作用幂等 · 引用溯源 · 拒答

砍掉任何一项，项目就退化成"又一个 RAG 聊天机器人"。

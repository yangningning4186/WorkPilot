# C｜质量-成本帕累托前沿

**日期**：2026-08-16
**数据集**：`core-dev` / origin=human，20 题
**跑批**：7 配置 × 3 重复 = 21 次，串行，`mode="evaluation"`（关 fallback、关缓存）
**产物**：`eval/outputs/pareto/20260816T135957Z/`

## 相对 docs/07 §4 的设计偏离

§4 的 C1–C8 按 `generate / plan / rewrite / judge` 四环节设计。本项目在线答题链路
只有**两个**模型环节，矩阵按真实链路重做为 2 因子（生成 × 证据门控）：

- **没有 `plan`**：固定综述是写死的六节点图，没有动态规划器
- **`rewrite` 不在链路上**：`query_decomposition_enabled` 默认关闭
- **`judge` 不在线上链路**：属评测侧，降档问题归实验 J

凑满八格测出来的是假数据。C8（全外部 API）因 external 未部署跳过。

## 测量口径

| 轴 | 口径 | 为什么 |
|---|---|---|
| 质量 | **规则轨**：拒答正确率 / 引用有效率 / 引用对齐率 / 约束通过率 | 六类 Judge 的人工标签未收口，用它当质量轴等于把未校准的仪器架在整张图下面 |
| 成本 | **GPU 秒与 token**，不折算美元 | 等价云单价是外部假设；"哪个配置在前沿上"只取决于相对关系 |
| 并发 | 串行（并发≈1） | §4 要求各配置在**相同**并发设定下测；串行也是一致设定。绝对数字不代表生产吞吐 |
| 门控预算 | 统一 1024 token | 默认 300 是按非推理模型调的，heavy 会烧在思考链上返回空 content。在某档位结构上够不到的预算下比档位，比的是预算 |

三次重复取**中位数**（不是均值：一次异常不会把结论拖走）。

## 结果

| 配置 | generate | gate | 拒答 | 引用有效 | 引用对齐 | 约束 | 错误 | 墙钟s | tok/题 | 延迟ms |
|---|---|---|---|---|---|---|---|---|---|---|
| C1 全重档 | heavy | heavy | 78.9% | 100% | 34.3% | **68.4%** | 1 | 369.3 | 4940 | 18003 |
| C2 全主力（基准） | main | main | 70.0% | 100% | 29.4% | 55.0% | 0 | 47.5 | 4469 | 2361 |
| C3 全轻档 | light | light | 56.2% | 100% | 26.3% | 31.2% | **4** | 40.0 | 2527 | 2169 |
| C4 生成降档 | light | main | 64.7% | 100% | 18.9% | 35.3% | **3** | 48.5 | 3104 | 2554 |
| C5 门控降档 | main | light | 65.0% | 100% | 29.0% | 45.0% | 0 | 40.5 | 4257 | 2014 |
| C6 生成升档 | heavy | main | 68.4% | 100% | **38.1%** | 57.9% | 1 | 113.4 | 4406 | 5658 |
| C7 门控升档 | main | heavy | **80.0%** | 100% | **14.7%** | 60.0% | 0 | 220.8 | 5276 | 11026 |
| C8 全外部 | — | — | 跳过：external 未部署 | | | | | | | |

## 五条结论

### 1. light 有硬容量边界，不是质量取舍

C3 每次固定 4/20、C4 固定 3/20 题**直接跑不了**：

```
You passed 6993 input tokens and requested 1200 output tokens.
However, the model's context length is only 8192 tokens
```

dev 集平均 11173 token/题，light 的 8192 上下文装不下。**15–20% 的题不是答得差，是根本
没答**——而剩下 80% 的指标是在"只保留了短题"的子集上算的，与其他配置并不同基。
分档不只是质量与成本的取舍，先是一道容量门槛。

### 2. heavy 在 temperature=0 下不可复现

只有含 heavy 的两个配置在三次重复间抖动，其余五个配置**逐位一致**：

| 配置 | 引用对齐三次值 | 跨度 |
|---|---|---|
| C1 | 0.429 / 0.343 / 0.333 | 9.6pp |
| C6 | 0.300 / 0.435 / 0.381 | 13.5pp |
| C2/C3/C4/C5/C7 | 完全相同 | 0 |

连续批处理下 temperature=0 也不保证确定性。**推论：任何用 heavy 产出的指标都必须
多次重复取中位数**，单次结果的可信区间比配置之间的差距还大。这条直接影响实验 J
（Judge 降档）的设计——基准 Judge 用 heavy 就必须重复多次。

差点因此得出错误结论：汇总最初只对墙钟取中位数、质量指标直接用 r1，
C1 的引用对齐显示为 42.9%（三次里最高的一次），实际中位数是 34.3%。

### 3. 约束通过率随生成档单调，代价是延迟

C1 68.4% > C7 60.0% > C6 57.9% > C2 55.0% > C5 45.0% > C4 35.3% > C3 31.2%。
从 C2 升到 C1 换来 +13.4pp，代价是墙钟 **7.8×**、单题延迟 2.4s → 18.0s。

### 4. 把 heavy 放在证据门控上是净负面 ★

C7（门控升档到 heavy）的引用对齐 **14.7%，全场最低**，比基准 C2 的 29.4% 腰斩，
也低于 C5（门控降到 light，29.0%）。代价还是墙钟 4.6×。

反直觉但方向一致：更强的门控模型判得更严，放行的证据集更窄，生成能引的原文随之
变少，引用与 gold span 的重合度就掉了。**门控的"强"与端到端的"好"不是一回事。**

对照 C6（只把生成升到 heavy）：对齐 38.1% 是全场最高，墙钟只有 C7 的一半。
同样是花 heavy 的钱，**花在生成上有回报，花在门控上是负收益**。

### 5. 全重档被生成升档支配

以引用对齐为质量轴，C1（369.3s，34.3%）被 C6（113.4s，38.1%）**完全支配**——
更便宜且更好。C4 被 C2 支配（更慢 1s 且约束低 19.7pp）。

前沿点（约束轴）：C3 → C5 → C2 → C6 → C1，其中 C3 因 20% 题跑不了应视为无效点。
**C5 与 C6 是两个真正有意义的选择**：想省就 C5（比基准快 15%、约束 -10pp），
想好就 C6（约束 +2.9pp、对齐 +8.7pp，代价墙钟 2.4×）。

## 一次数据污染的复盘

第一版矩阵里 C3 的墙钟是 1.6 秒、0 次调用、20 道题——物理上不可能。查库发现两条
同名 `pareto-C3-r1` 批次：一条是被我中断的那轮留下的空壳，一条是真实数据。
`load_batch_costs` 按 **label** 过滤并取第一条，于是取到了废数据。C2-r1 也被同样
污染，只是它的数字"看起来合理"，**不会被发现**。

两个错叠在一起才让它暴露：汇总当时只渲染 `repeat<=1`，把三次重复的意义整个丢掉，
而三次重复恰恰是防这个的。

修正：程序化取用一律按 `batch_id`（label 是人给的名字，重跑必然撞车）；汇总改为
按配置聚合三次取中位数；补 `test_same_label_from_a_rerun` 用真实数据库钉死。

## 边界

- 20 题小样本，配置间几个百分点的差距不构成显著性结论；本次只做方向性判断，
  未跑 bootstrap 置信区间
- 引用对齐是**自动代理指标**（quote 覆盖 gold span 达 theta），不等于语义引用准确率
- 串行口径，不反映生产并发下的成本
- C8 缺席，"自建 vs 商用"的量化对比仍是空的
- C3/C4 的指标建立在被截断的子集上，与其他配置不同基，不应直接比较

## 下一步

1. 实验 J（Judge 降档）——受结论 2 约束，基准 Judge 必须多次重复
2. 给 light 单独设一条短证据链路，或直接承认它不适合 RAG 生成，只用于分类/改写
3. 结论 4 值得单独归因：抓 C7 与 C2 在同一批题上的门控判定差异，确认"判得更严 →
   证据更窄 → 对齐更低"这条链路

# G1 · 首份 baseline 快照与夜间门禁点亮

| | |
|---|---|
| **日期** | 2026-08-17 |
| **git_sha** | `92dd6b8`（跑批）；门禁修复与本台账在后续同一提交 |
| **数据集** | `m1-dev-70`，70 条 human，`heading` 分块。**留出集未访问** |
| **变量** | 无。本轮不做对照，只用生产配置产出可提交的 baseline |
| **产物** | `eval/snapshots/retrieval.json`、`eval/snapshots/generation.json`，及 `eval/outputs/dev-suite-{retrieval,generation}/*-20260817/` |
| **相关文档** | [06 评测体系 §4](../06-评测体系.md)、[G0 门禁准备](2026-08-17-G0-门禁准备与噪声地板.md)、[E9 HNSW 收口](2026-08-17-E9-HNSW参数与topk收口.md) |

> G0 把静态轨挂上了 CI 并算出噪声地板，`gate.py` 也写完了，但**没有 baseline 快照，
> 门禁就是空转的**——`--against main` 读不到文件，每次都直接拒判。
> 本轮做的就是把它点亮：产出首份快照，并且**实跑通过、阻断、拒判三条路径**，
> 而不是"工具写完了就算完成"。

---

## 1. 为什么重跑而不是直接用 8/16 的批次

`m1-dev70-finalists`（8/16）就是 E1 的正式批次，数字可用。但 baseline 快照会被提交进 git，
成为此后每次改动的判据，所以它必须满足两条：

1. **git_sha 等于当前 HEAD**。8/16 之后合入了 E5 证据门控修复、E8 阈值结论、E9 参数收口，
   快照挂在旧 sha 上，日后回看无法判断"这份基线是哪套代码跑出来的"。
2. **只含生产配置**。finalists 批次同时跑了 `heading` 与 `semantic` 两套，
   是对照产物；baseline 只应该是生产那一套。

代价是一次本机跑批（70 条，约 5 分钟，全本地模型，零外发、零费用）。

### 顺带得到一次复现验证

| | span Recall | budget Recall | nDCG | α-nDCG | MRR | ctx precision |
|---|---:|---:|---:|---:|---:|---:|
| 8/16 原批 / E9 baseline | 0.7924 | — | 0.7117 | — | — | 0.1035 |
| 本轮（HEAD `92dd6b8`） | **0.7924** | 0.8012 | **0.7117** | 0.7117 | 0.7116 | **0.1035** |

span 诊断分布同样逐位一致：`hit 64 / outside_top_k 15 / relevant_chunk_not_ranked 8`。
跨 12 小时、跨若干次合入，检索轨**逐位可复现**——这正是 E9 §2 的推论：
当前语料规模下规划器走顺序扫描，dense 臂实际是精确检索而非近似，没有随机性来源。

失败样本 0 条。延迟 mean 4338ms / p95 4765ms（不进门禁，理由见 G0 结论 3）。

---

## 2. 快照的隐私审计

快照要进 git，所以逐字段扫过一遍（约束 7 与 06 §4.2 的白名单设计）：

- 70 条，246KB，字段只有数字、UUID、`gold_fingerprint`（sha256 前 16 位）
- 全文正则扫描 **0 处** CJK 字符、**0 处** 超 80 字符的字符串
- `answer` / `gold_answer` / `citations[].title` / `span_diagnostics[].quote` /
  `source_uri` / `document_id` 均不存在

白名单而不是黑名单的价值在这里兑现：`retrieved[]` 里留下的是 `chunk_id` / `version_id` /
`char_start` / `char_end` / `content_tokens`，**位置留了、原文没留**，
既能判回归又不把语料带进版本库。

---

## 3. 三条路径实跑验证

工具能跑 ≠ 门禁在工作。三条路径各跑一次真报告：

| 路径 | 输入 | 期望 | 实际 |
|---|---|---|---|
| 通过 | baseline 跑批自比 | 退出码 0 | ✅ 全部 +0.0000，成本 +0.0% |
| 阻断 | E1 的 `semantic` 跑批 | 退出码 1 | ✅ 5 项指标回退，逐项列出 |
| 拒判 | `core-dev` 的 M0 跑批（数据集不同） | 退出码 2 | ❌ **traceback + 退出码 1** |

第三条挂了。见下节。

### 阻断路径抓到的正是 E1 那个取舍

`semantic` 候选被拦下的同时，成本一项是 **-16.8%**：

| 指标 | baseline(heading) | semantic | 判定 |
|---|---:|---:|---|
| span_recall_at_k | 0.7924 | 0.7719 | 回退，阻断 |
| ndcg_at_k | 0.7117 | 0.6541 | 回退，阻断 |
| mrr | 0.7116 | 0.6547 | 回退，阻断 |
| retrieved_tokens | — | **-16.8%** | 通过（上限 +20%） |

这就是门禁设计意图的实证：**它不阻止你做取舍，它逼你把取舍显式化**。
想让 semantic 上线，正确动作是重新生成 baseline 并把这张表写进台账，不是放宽阈值。

---

## 4. 抓到的真 bug：拒判与不合格撞了退出码 ★

`gate.py` 的模块文档写得很清楚：

> fail-closed：候选跑批含失败样本、gold span 指纹与 baseline 不一致、**数据集不一致、
> 受控配置漂移**，一律拒绝判定（**退出码 2**），与"判为不合格"（退出码 1）分开。

四个条件里**只有前两个真的这样做了**。原因是异常来自两个层：

- `_reject_incomplete` / `_reject_gold_drift` 在 `gate.py` 里，抛 `GateRefused` → 退出码 2 ✅
- 数据集不一致、受控配置漂移、`item_id` 配不上，都在 `compare.py` 的
  `check_compatibility` / `pair_items` 里，抛 `ValueError` → `main()` 不接 → 
  **traceback + 退出码 1** ❌

`ValueError` 对 `eval.compare` 是合适的：那是人在终端手工比两次跑批，看见 traceback 就改命令。
但门禁是给夜间自动化用的，退出码是唯一契约。撞码的后果很具体：
**把"跑批配错了"报成"质量回退"**——最坏的情况是有人为了让门禁变绿去改检索代码，
而真正的问题只是传错了一个目录。顺带还违反约束 4：吐给调用方的是 stack trace 而不是可执行说明。

**修法**：在 `gate.py` 里把比较层的 `ValueError` 统一转成 `GateRefused`，
不改 `compare.py` 的对外口径（那条链路的语义是对的）。候选报告本身载入失败同样归入拒判。

修复后：

```
$ eval.gate check <core-dev 的跑批> --against working
门禁拒绝判定：数据集不一致，配对比较无效: baseline=m1-dev-70, candidate=core-dev
退出码=2
```

已按棘轮登记为 `G1-门禁拒判退出码撞车`，四条用例锁住（`test_eval_gate.py`）。
其中两条原本断言的就是 `ValueError`——**测试把错误的契约钉住了**，
所以本轮同时改了断言。这也是个教训：测试写的是"当前行为"还是"应有行为"，
在异常路径上特别容易搞混。

---

## 5. 生成轨：补上缺失的合并步骤

owner 在同一轮明确授权模型外发后补跑了生成轨。外发范围逐项核实并写进 manifest
（含授权说明的 sha256 指纹）：70 条问题与截断证据 → 自建集群 main 档
`qwen3.6-35b-a3b`；`TIER_EXTERNAL_BASE_URL` 为空，不经任何商用 API，数据不出内网；
`mode="evaluation"` 强制关闭档位 fallback 与精确缓存。

### 拦路的不是模型，是一个缺失的合并步骤

检索轨 runner 一直会把四份分 dataset 的子报告并成一份 70 条的整套报告，
**生成轨 runner 没有这一步**。于是生成轨只有四份分 dataset 报告，门禁没有可判的对象。

按 dataset 分别过门禁不是等价替代：70 条碎成四组后每组配对样本更少，
快照也要维护四份。所以给生成轨补上了同构的 `_combine_reports`，其中两处值得记：

- **聚合口径直接借 `report_metrics` 的 MetricSpec**，不另写一份求和逻辑。
  门禁判的就是这些 spec；报告里的聚合值与门禁读到的必须是同一个定义，
  否则"报告上没退、门禁说退了"迟早发生。
- **`annotation_fingerprint` 逐 dataset 不同，但不能当作噪声丢掉**。
  它是 `constraint_pass` 的判据身份。做法是排除出漂移比对，但合成一个总指纹并把
  各自原值写进 `chunk_metadata`——标注改过仍然显形。除这几个天生逐 dataset 的字段外，
  任何配置项不一致仍然直接拒绝合并。

### 首批有一条失败样本

`table` 类的一条报 `RemoteProtocolError: Server disconnected`，集群瞬时断连，非数据问题。
门禁的 `build_snapshot` 拒绝含失败样本的跑批（这正是它该做的），所以整批重跑。
第二批 70 条全部完成，实际模型身份唯一为 `qwen3.6-35b-a3b`（没有 fallback 漂移）。

### 生成轨 baseline

| 指标 | 值 | 可比样本 |
|---|---:|---:|
| `refusal_correct` | 0.8143 | 70 |
| `citation_validity_non_refusal` | **1.0000** | 44 |
| `constraint_pass` | 0.5714 | 70 |
| `constraint_pass_answerable` | 0.4737 | 57 |
| `citation_gold_alignment` | 0.3097 | 44 |
| `total_tokens`（均值） | 15110 | 70 |

`citation_validity` 满分而 `citation_gold_alignment` 只有 0.31，与 M0 台账那条结论同构：
**规则轨查得出"引用格式合法"，查不出"引用真实但不支撑论断"**。
这个缺口要靠 Judge 补，不是靠调规则。

### 快照按轨解析

两条轨的指标集完全不同，共用一个 `baseline.json` 早晚有人拿生成轨候选去比检索轨基线。
所以快照改成按报告类型自动解析：`eval/snapshots/retrieval.json` 与
`eval/snapshots/generation.json`，`--baseline` 仍可覆盖。

生成轨快照的隐私审计比检索轨更要紧——原始报告里 `answer` / `gold_answer` / `question` /
`citations[].title` 全是逐字原文。白名单收窄后：`citations` 只剩 `citation_id`（如 `S18`），
全文扫描 **0 处** CJK、0 处超长字符串，60KB。

### 生成轨的三条路径

| 路径 | 输入 | 结果 |
|---|---|---|
| 通过 | v2 自比 | ✅ 退出码 0 |
| 拒判 | v1（含 1 条失败样本） | ✅ 退出码 2，点名失败样本 |
| 阻断 | —— | ⚠️ **未用真报告验证** |

诚实标注：生成轨的阻断路径目前只有 fixture 测试（`test_generation_track_is_gated_too`）覆盖，
没有真实回退报告实跑过。手上没有第二份同 70 条、同配置的生成跑批可用于制造真实回退，
不打算为了填表去造一个假报告。检索轨的阻断路径是用 E1 的 `semantic` 真报告验的。

---

## 6. 结论与边界

**一句话结论**：检索轨与生成轨的首份 baseline 快照均已提交，门禁在两条轨上都生效；
过程中修掉一个会把配置错误误报成质量回退的退出码 bug，并补上生成轨缺失的报告合并步骤。

**当前门禁的覆盖边界**（不写清楚就会被误读成"质量已经被全面挡住"）：

- **规则轨指标，不含语义正确性**。`answer_correctness` / `faithfulness` /
  `citation_accuracy` 仍是 pending：J2 已校准 binary correctness，但快照还不携带 Judge 字段。
  生成轨的 `citation_validity` 满分而 `citation_gold_alignment` 只有 0.31，
  正说明规则轨查不出"引用真实但不支撑论断"——这个缺口只能由 Judge 补。
- `refusal_correct_at_threshold`（检索轨）进了门禁，但**当前几乎没有分辨力**：
  阈值 0.35 下 `true_refusal = 0 / false_refusal = 0`，13 条不可答全部由证据门控（生成轨）
  拦下，分数门一条都没贡献（E8 §2 已证明这条参数是死的）。它现在恒为 57/70 = 0.8143，
  只有当 `top_score` 跨过 0.35 才会动。**留着不是因为它有用，是因为它变了就说明分数分布变了。**
- **生成轨的阻断路径尚未用真报告验证**（见 §5），只有 fixture 覆盖。
- 语料规模一变（E9 §2：规划器改用向量索引后），检索轨的逐位可复现性会消失，
  届时零回退阈值必须按新的噪声地板重定。生成轨本就有模型采样噪声，
  其零回退阈值依据的是 G0 测得的"light/main 档三次重复逐位一致"，
  换模型或换采样参数后必须重测。

---

## 7. 遗留问题

- [ ] 把 J2 校准过的 `answer_correctness` 接进 generation 快照字段，解除 pending
- [ ] 生成轨阻断路径：等下一次真实有回退的跑批出现时补验，不造假报告
- [ ] 真正把 gate 挂进夜间定时任务（现在是手工跑）
- [ ] `agent_task` 升到七类正式校准后，评估是否需要第三条轨的快照

# A5 · 长期记忆注入工程对照（2026-08-18）

## 结论

在 10 条冻结的 synthetic seed 上，使用同一模型、温度和 token 上限做严格配对对照：
`memory_off` 的词面规则成功率为 0/10，`memory_on` 为 9/10，配对增益 +0.90，
bootstrap 95% CI 为 [+0.70, +1.00]。这证明长期记忆注入链路在工程 smoke 中能稳定
改变回答，但不证明真实 owner 的产品满意度。

评测包的 `claim_scope` 固定为 `engineering_only_no_product_claim`，owner 盲评仍为
pending，不由脚本代填。

## 授权与数据边界

- owner 于 2026-08-18 在 Codex 会话中明确授权，仅发送
  `eval/suites/a5-memory-seed.json` 中的 10 条合成样本。
- 目标端点为 `http://172.16.6.66:3270/v1`，请求模型为 `qwen3.6-35b-a3b`。
- 未读取、未发送真实 owner 记忆、本地文档或项目数据。
- 评测使用 `evaluation_no_fallback`，不允许路由失败后换模型。
- 额外的 `/models` 只读检查通过：端点列表仅包含
  `qwen3.6-35b-a3b`；没有为身份检查发送额外 chat prompt。

## 固定协议

- suite SHA-256：`647374ccb03871b158a7d4fb46b3d432c96cdf723beb29611343f9c7475ff92e`
- system prompt SHA-256：`fe1d27989693077675772a0b909d7a0c10614f46955b87aa2448bc76cc9016ef`
- 运行时 Git SHA：`cf69c7e`
- 样本数：10；每条分别调用 `memory_off` / `memory_on`，调用顺序逐条交替
- 温度：0.0；`max_tokens=500`
- 统计：配对 bootstrap，固定 seed `20260818`，5000 次重采样

## 结果

| 指标 | memory_off | memory_on | Δ |
|---|---:|---:|---:|
| 词面规则成功率 | 0.00 | 0.90 | +0.90 |
| 平均延迟 | 852 ms | 559 ms | -293 ms |
| 平均输入 token | 78.9 | 125.6 | +46.7 |

延迟只是这一次 10 条小样本的运行观察，不宣称记忆注入会降低线上延迟。

## 失败样本复核

`a5-003` 要求按“先给结论，再列两条依据”组织回答，冻结规则要求字面同时包含
“结论”和“依据”。`memory_on` 回答实际先给了一句概括，随后列出两个编号要点，
但没有逐字使用这两个标签，因此被规则轨判失败。这更像保守词面规则的假阴性，
不应在看到结果后修改 suite 把它抹掉；是否真的满足偏好留给 owner 盲评。

## 盲评与产物校验

- `blind-review.jsonl` 为 10/10 条，不含任何 `memory_on/off` 臂名。
- A/B 按样本序号交替映射，已与 `records.jsonl` 逐条校验。
- owner 需填写 `preferred`、两臂 1–5 分、`reviewer` 和 `reviewed_at`；缺项时
  `eval.memory_blind_review` 会 fail-closed，不生成满意度结论。

原始产物在 Git 忽略目录
`eval/outputs/memory-injection/a5-seed-qwen36-20260818-authorized/`：

| 文件 | SHA-256 |
|---|---|
| `report.json` | `151e00a46113dd94ca8bc1d8800a83f2db58e0894e9b5fcaa1a773bcf27e54b0` |
| `records.jsonl` | `5b027e2d5f4d2d276e23417f7e495cea71e3612b0aca2d2dd4e0e5d01c037751` |
| `blind-review.jsonl` | `68fc1f7919b2c56adca3cd8d03f7beb5cd633551ab6912e2ccdc26b6d5849503` |
| `endpoint-check.json` | `08b357a4a26ac1b7d693b0aede1bf46321c0a7f104481d44c479074311a8def3` |

## 决策

1. A5 工程 smoke 结案：记忆注入的确改变了合成个人化任务的结果。
2. 不把 synthetic 9/10 写成产品成功率，不用脚本代替 owner 盲评。
3. 真实 owner 满意度和 human suite 属于后续产品验证，若需向外部端点发送，必须另行授权。

# E4 · 70 条 dev 扩展评测收口（2026-08-16）

## 结论先行

70 条 human dev 已在不访问 10 条隔离 test 的前提下完成正式检索与 heading 生成跑批，
两轨均为 70/70、0 error。检索对照不足以推翻现有默认：`heading` 的质量点估计更高，
`semantic` 的质量差异置信区间跨 0；但 semantic 显著减少上下文 token 和延迟，继续保留为
效率候选。

生成轨暴露出新的主矛盾：13/13 条 unanswerable 全部正确拒答，但 57 条 answerable 只回答
36 条，21 条被证据门控误拒。当前不能只调检索分数阈值，因为检索阈值与模型证据门控给出了
相反方向的信号；下一轮应先对 21 条误拒做证据门控归因。

六类 Judge 校准包已经冻结为 70 个唯一 case，六类覆盖完整，稳定拆分为 calibration 51 / 
validation 19。Judge 模型跑批仍待人工 0/1/2 正确性标签和正式 heavy 模型接入；这份中间结论
不包含 `agent_task`，也不触碰 test。

## 数据与纪律

| 项目 | 固定值 |
|---|---|
| suite | `eval/suites/m1-dev-70.json` |
| origin | `human` |
| dev | 70 |
| test 访问 | 0；manifest 明确记录 `test_datasets=[]` |
| 类别 | single 19 / multi 14 / table 12 / unanswerable 13 / temporal 6 / global 6 |
| `agent_task` | 0；真实 Agent 执行闭环前不造占位题 |
| 检索链路 | dense + lexical RRF + bge rerank |
| 对照变量 | `heading` vs `semantic` |
| 生成策略 | `heading` |
| 生成模型 | `openai_compatible/qwen3.6-35b-a3b` |
| 发送范围 | 70 条 dev 问题与截断检索证据；用户于 2026-08-16 明确授权内网端点 |

suite 定义 SHA-256：
`3c5f4719a7c2844ce4c97c3ab9f36f8eff4aacefd043e3b48bb6e3d49ee5ec6e`。

检索 manifest：
`eval/outputs/dev-suite-retrieval/20260816T020650.974049Z-m1-dev70-finalists-20260816/manifest.json`。

生成 manifest：
`eval/outputs/dev-suite-generation/20260816T021952.089755Z-m1-dev70-heading-generation-20260816/manifest.json`。

## 检索结果

| 指标 | heading | semantic | semantic - heading / 95% CI |
|---|---:|---:|---:|
| span Recall@10 | 0.7924 | 0.7719 | -0.0205 `[-0.1045, 0.0556]` |
| budget span recall | 0.8012 | 0.7807 | -0.0205 `[-0.1058, 0.0606]` |
| nDCG@10 | 0.7117 | 0.6541 | -0.0576 `[-0.1377, 0.0202]` |
| MRR | 0.7116 | 0.6547 | -0.0569 `[-0.1533, 0.0358]` |
| context precision | 0.1035 | 0.1053 | +0.0018，区间跨 0 |
| Top-K token/条 | 4211.4 | 3503.9 | **-707.5 `[-978.5, -445.8]`** |
| 延迟/条 | 4275.6ms | 4006.4ms | **-269.2ms `[-356.3, -202.1]`** |
| 拒答 AUROC | 0.8273 | 0.8583 | 仅方向性比较 |

配对矩阵：
`eval/outputs/strategy-matrix/m1-dev70-finalists-20260816/report.json`。样本集合、类别、
answerability 与 gold span 指纹全部严格配对；8 条策略分歧、4 条共同为 0、38 条共同满分。

### 检索分数阈值发现

配置阈值 0.35 在两套 rerank 结果上都拒答 0/13 条 unanswerable，不能继续解释为有效的
score-only 拒答阈值。dev 最优点随分块变化：heading 约 0.5986，semantic 约 0.5828。
这说明阈值需要按真实排序分数重新定义/校准，不能直接把某个 dev 最优数抄进生产配置。

## heading 生成结果

| 指标 | 结果 |
|---|---:|
| 完成 / error | 70 / 0 |
| 拒答决策正确 | 49/70 = 70.0% |
| answerable 实际回答 | 36/57 = 63.2% |
| unanswerable 正确拒答 | 13/13 = 100% |
| citation validity | 70/70 = 100% |
| 非拒答 citation validity | 36/36 = 100% |
| constraint pass | 31/70 = 44.3% |
| citation-gold alignment 代理 | 39/139 = 28.1% |
| 端到端平均延迟 | 6169.5ms/条 |
| token | 881,283，总计；12,589.8/条 |
| 金额成本 | 不可用；自部署价格表为 0，只报告 token |

分类型看，`global` 最弱：6 条只回答 2 条，constraint 0/6；`temporal` 回答 5/6；
unanswerable 13/13 正确拒答。34 次拒答中，33 次为 `model_insufficient_evidence`，1 次为
`evidence_gate_invalid`。因此下一轮 badcase 归因应优先检查 global、多跳/表格证据在 gate 输入中
是否被截断、重排或表达不足。

`citation_gold_alignment` 只是 quote 覆盖 gold span 的自动代理，不是语义引用准确率；
answer correctness 也必须等人工标签与校准 Judge，不能由上述规则指标替代。

## Judge 六类校准包

目录：`eval/outputs/judge-calibration/m1-dev70-six-class-20260816/`。

| 检查 | 结果 |
|---|---|
| 唯一 case | 70/70，重复 `(dataset,item_id)` 为 0 |
| 六类覆盖 | 19 / 14 / 12 / 13 / 6 / 6，完整 |
| calibration / validation | 51 / 19；两边六类均有覆盖 |
| rubric fingerprint | `c06fb1a…928da63` |
| prompt fingerprint | `9870b97b…616729f` |
| 当前状态 | 等待人工标签与 heavy Judge 接入 |

生成授权与 Judge 授权分开记账：本轮 Qwen 生成授权已经落在 generation manifest；
Judge 包仍保持 `model_send_authorized=false`，不会把 main 模型冒充 heavy，也没有 fallback。

## 决策与下一步

- 保留 `heading` 为默认；semantic 继续作为效率候选，不宣称质量胜出。
- 不修改生产拒答阈值；先拆解 score-only 阈值与模型 evidence gate 的职责和分数来源。
- 先人工标注 70 条 answer correctness，再用真实自部署 DeepSeek heavy 端点跑 Judge 校准；
  validation 整体 QWK / accuracy 达标后才允许用 Judge 出质量结论。
- 并行推进固定综述工作流；其真实工具 schema 与执行产物闭环后再补 `agent_task`，升级七类。
- 10 条 test 继续隔离，到最终方案冻结后只跑一次。

后续 evidence gate 归因、修复与同套件受控复跑见
[E5 · Evidence gate 误拒归因与修复](2026-08-16-E5-evidence-gate误拒修复.md)。E4 保留修复前基线，
不回写覆盖历史数字。

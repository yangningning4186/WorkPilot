"""消息面：把审批与提问**送出去**，把频道消息与 @提及**带进来**。

四个方向分明的部件，别把它们搅在一起：

- `routing`（出站）：命名 Inbox + 投递绑定。应用内 Inbox 永远是 store of record，
  绑定只是把同一条 item 镜像到一个聊天频道；点击卡片按钮就地解析同一条 item。
- `subscriptions`（入站）：会话订阅一个频道，频道消息推进来唤醒它。
- `mentions`（入站）：@机器人时开一个会话并让它**拥有**那条 thread。
- `unrouted`（可见性）：无处投递的入站消息与失败的后台轮次的死信存储。

**别把 Inbox 绑定和订阅指向同一个频道。** 一个把审批发出去，一个把消息听进来；
指到一起就是自问自答的回路。

传输是可换的：`routing` 只要一个 `sender` 可调用对象，`buttons` 只认 `(label, value)`。
目前只实现了飞书适配器（`feishu`），因为它是本项目已有官方连接器、能发交互卡片、
也能回推事件的那一个。
"""
